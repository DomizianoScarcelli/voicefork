import {StyleSheet} from 'react-native'
import {Colors} from '../../constants'

export const homepageText = StyleSheet.create({
    baseText: {
        fontSize: 10,
        color: 'black',
    },
})

export const homepageColor = StyleSheet.create({
    primary: 'black',
    secondary: 'white',
})

export const homepage_style = StyleSheet.create({
    main_view: {
        backgroundColor: Colors.white,
        width: '100%',
        height: '100%',
        padding: 15,
    },
})
