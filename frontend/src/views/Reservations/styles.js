import {StyleSheet} from 'react-native'
import {Colors} from '../../constants'

export const reservationsText = StyleSheet.create({
    baseText: {
        fontSize: 10,
        color: 'black',
    },
})

export const reservationsColor = StyleSheet.create({
    primary: 'black',
    secondary: 'white',
})

export const reservations_style = StyleSheet.create({
    main_view: {
        backgroundColor: Colors.white,
        padding: 15,
    },
})
