type Restaurant = {
    id: string
    name: string
    image: string
    cuisine?: string
    averagePrice?: number
}
type LatLng = {
    latitude: number
    longitude: number
}
export type {Restaurant, LatLng}
