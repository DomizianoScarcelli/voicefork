export enum TileType {
    RESTAURANT = 'RestaurantTile',
    CUISINE = 'CuisineTile',
    RESERVATION = 'ReservationTile',
    EMPTY = 'EmptyTile',
export enum SearchStrategy {
    NEARBY,
    KEYWORD,
    RATING,
}
